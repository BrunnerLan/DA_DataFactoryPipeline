# DA_DataFactoryPipeline
The pipeline copies data from the blob storage and places it on Sharepoint as a backup on the one hand and on the other hand it is processed and implemented in an SQL server 
